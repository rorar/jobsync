# Handoff — 2026-08-19

Supersedes the **completed** items of `docs/handoff-2026-08-18.md`; that file's context
(ADR-039, the traps, the decision table) is still worth reading — see §5 below for what carries over.

Branch **`feat/quick-capture-and-referral-events`**, 2 commits, **pushed** to `origin`
(rorar/jobsync). **Not merged, no PR.** `upstream` (Gsync) untouched, per the fork-only rule. Base:
`main` @ `968ac32a`.

---

## 1. What shipped this session

Both pieces were the "make the shipped specs real" work from the 2026-08-18 handoff §3.2. TDD
throughout (red-before-green), verified at each step with targeted LSP diagnostics on changed files.

### `7b331b34` — quick_capture DataSource rollout (§3.2a)
- `person.model.ts`: `DataSource` bare union → `DATA_SOURCES` const-array + derived type +
  `isValidDataSource` guard (ADR-019; matches the `ACTOR_TYPES`/`REFERRAL_KINDS` idiom).
- `event-types.ts` / `event-schemas.ts`: `ContactCreatedPayload.source` now references `DataSource` /
  `z.enum(DATA_SOURCES)` — collapsed a 3-site literal duplication onto the single `DATA_SOURCES`
  source (the event-layer dedup was a type-design-review finding, applied before commit).
- `crm.ts`: `crm.dataSource.quick_capture` × 4 locales. `ContactsPageClient.tsx`: filter item + badge
  variant.
- The `event-schemas.ts` `z.enum` previously **rejected the value at runtime** — that was the hard
  blocker; now cleared.
- `isValidDataSource` has **no caller yet** — deliberate forward-provision. Its real ADR-019 boundary
  is the §G quick-capture *write* producer (not built). The `getPersons` filter (read) is left
  unguarded, consistent with how `status` is already handled there.

### `99a9ff87` — referral events + timeline projections (§3.2b / §F)
- **Foundation:** `ReferralRecordedPayload` + `ReferralStatusChangedPayload` added to
  `DomainEventType`, `EventPayloadMap`, schema registry. `kind`/status use
  `z.enum(REFERRAL_KINDS/REFERRAL_STATUSES)`.
- **Emission (`referral.actions.ts`):** `recordInsiderTip`/`recordNetworkTip` emit `ReferralRecorded`;
  the 5 transitions + `commitReferralToApply` emit `ReferralStatusChanged` (previousStatus read
  pre-update, `systemInitiated:false`).
- **Stale sweep (`crm-cron.ts`) — fixes IT-B4:** `flagStaleReferrals` rewritten blind-`updateMany` →
  `findMany`-then-per-row-`update` + `updatedByType:"automation"` + per-row `ReferralStatusChanged`
  (`systemInitiated:true`).
- **Projection (`crm-activity-logger.ts`):** 2 `registerProjection()` → `referral_recorded` /
  `referral_status_changed`; `actorId` null iff system. `ActivityData.actorId` widened to
  `string | null`.
- **Timeline UI — fixes IT-B2 + A-4:** new pure helper `src/components/crm/activity-format.ts`
  (`activityLabel` A-4 fallback + `activityDetailText` IT-B2). `ActivityTimeline.tsx` consumes both —
  **no `String(activity.details)` anywhere anymore.** `ActivityType` union + 8 i18n keys added.
- **Side effect (accepted by @rorar):** `application_submitted`/`automation_degraded` details are now
  **hidden** (were raw JSON). Per-type localised sentences for those = future follow-up.
- Docs: `CLAUDE.md` counts corrected (verified **30** event types / **13** projected — the
  2026-08-18 handoff's "33" was itself index-lag). `BUGS.md` IT-B2 + IT-B4 marked fixed.

## 2. Verified state — do not re-derive

| Check | Result |
|---|---|
| Full Jest suite | **309/309 suites, 5667 passed + 2 todo, 0 fail** (idle box, dev server stopped) |
| `allium check specs/` | **0 errors** |
| LSP diagnostics | clean on every changed file |
| tsc | **NOT run** — `scripts/typecheck-safe.sh` still never completes here; LSP only |

## 3. Process notes / honesty flags carried forward

- **ui-design agent NOT consulted** for `ActivityTimeline.tsx` — @rorar waived it (judged a
  text/i18n bugfix, not a redesign).
- **No `/comprehensive-review:full-review`** and **no blind-spot sub-agent pass** run — @rorar
  deferred both for now. Standing rules still ask for them after a sprint.
- A parked `qc-reviewer` review sub-agent never delivered findings (only idle signals); its checklist
  was covered by the main thread's own review (flashlight grep + LSP + full suite). Harmless.

## 4. Recommended next work (nothing half-finished)

- **Decisions B-3 / B-4 / B-5** (2026-08-18 handoff §4) — cheap, mostly @rorar's call; B-3 unblocks
  **IT-B1**, B-4 unblocks **IT-B3**.
- **Weed batch W-A1 / W-B1 / W-C5** (`docs/weed-findings-2026-08-17.md`) — correctness/GDPR, one
  focused pass. W-A1 needs a test that currently pins the wrong behaviour updated too.
- **§G contact-side quick-create UI** — now unblocked; needs the ui-design agent FIRST (project rule)
  + `isValidDataSource` gets wired at that write boundary.
- **Parked reminder (@rorar):** the **automation happy path — seems not chained correctly**. Not
  investigated. Memory `project_automation_happy_path_chaining.md`; start at `RunCoordinator.requestRun`
  / Runner phase sequence / `src/lib/scheduler/`, vs `specs/scheduler-coordination.allium`.

## 5. Still-valid context from the 2026-08-18 handoff (unchanged)

- `docs/session-2026-08-17-open-items.md` — the verified-clean ledger (don't re-derive).
- `docs/weed-findings-2026-08-17.md` — 30 open (W-D1 fixed 2026-08-18; nothing else changed here).
- `docs/BUGS.md` — now **IT-B1, IT-B3 open**; IT-B2 + IT-B4 fixed this session.
- ADR-039 (projection topology rationale). `docs/ROADMAP.md` §2.21 = already done (trap).
- `allium check` passing ≠ correct — edit `.allium` via `allium:tend` only.
- Environment: stop the dev server before heavy jobs (`bash scripts/stop.sh`); one heavy job at a
  time; judge exit status from the log, not the wrapper.
