# Implementation Plan: undoStore split-brain full pipe-through (M-A-09)

**Track ID:** undostore-pipethrough_20260601
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-01
**Status:** ⛔ ENTRY-CRITERIA-GATED — [ ] Not Started

## Overview

~2–3 day focused sprint. **Phase 0 is the entry gate** — the ADR-030 amendment + migration
plan must land first. Then thread the token server→client→undo, widen the allowlist, and
verify undo compensates the server effect. TDD; the additive-contract guarantee is the
high-risk invariant (no existing caller may break).

## Phase 0: Entry gate — ADR-030 amendment (BLOCKING)

### Tasks

- [ ] Task 0.1: Draft the ADR-030 amendment: `onAction` →
      `{ success, createdJobId?, undoTokenId? }` (additive); define the token lifecycle +
      cross-user non-guessability (ADR-015). Plan the in-flight-undo migration.
- [ ] Task 0.2: Record the amendment via `/architecture-decision-records`. Track stays
      `pending` until it lands.

### Verification

- [ ] Amendment ADR committed; contract + migration agreed.

## Phase 1: Server — return undoTokenId for Job-producing actions

### Tasks

- [ ] Task 1.1: Unit test: `promote`/`superlike`/`block` register a compensation in
      `undoStore` and return its `undoTokenId` in `ActionResult.data` — failing first.
- [ ] Task 1.2: Register the compensations + return the token in
      `src/actions/stagedVacancy.actions.ts` (ADR-015 userId).

### Verification

- [ ] The three actions return a working `undoTokenId`; compensation registered.

## Phase 2: Client — thread the token through useDeckStack → UndoEntry

### Tasks

- [ ] Task 2.1: Test: `useDeckStack.onAction` forwards `undoTokenId` into the recorded
      `UndoEntry`; `{ success }`-only callers still compile (additive regression).
- [ ] Task 2.2: Widen the `onAction` return type + populate `UndoEntry.undoTokenId`
      (revive the dead field at `useDeckStack.ts:59`).

### Verification

- [ ] Token reaches `UndoEntry`; additive contract preserved.

## Phase 3: Undo — compensate + widen the allowlist

### Tasks

- [ ] Task 3.1: Component test: undoing a promote/superlike/block reverses client state
      AND calls `undoStore.compensate(tokenId)` (no orphan Job; super-like celebration removed).
- [ ] Task 3.2: Call `compensate(tokenId)` in `handleDeckUndo`
      (`StagingContainer.tsx:503-532`); add `promote`/`superlike`/`block` to
      `REVERSIBLE_DECK_ACTIONS`; update/remove the `:532` drift-guard accordingly.
- [ ] Task 3.3: E2E happy-path: superlike → undo → the created Job is gone.

### Verification

- [ ] Three actions reversible; undo compensates server effect; E2E passes; drift-guard consistent.

## Final Verification

- [ ] Entry-gate amendment ADR committed; token piped server→client→undo
- [ ] `{ success }`-only callers unaffected (additive); no orphan Job after undo
- [ ] `bash scripts/test.sh` green; `bun run build` zero type errors
- [ ] Ready for review

---

_Generated by Conductor. Entry-gated — Phase 0 (ADR-030 amendment) must clear first._

## Phase 4: Wrap-Up (finale Phase — vor Merge/Push)

Per `conductor/workflow.md` § Wrap-Up-Phase. Run only after all prior phases pass.

### Tasks
- [ ] Task 4.1: Blind-Spot-Analyse — projektweit `grep` nach den Pattern-Fixes dieses Tracks; adjacent Lücken schließen.
- [ ] Task 4.2: `/comprehensive-review:full-review` (Architecture+Security+Performance+Testing+Best-Practices) — alle realen Findings autonom fixen; Agent-Claims gegen `git diff`/Code verifizieren (kein Fabrizieren).
- [ ] Task 4.3: `/understand` inkrementell-Refresh + Graph-Commit (1× am Welle-Ende, NICHT per-Commit; `autoUpdate` OFF).
- [ ] Task 4.4: Honesty-Gate voll ausführen (2 Fragen: Shortcuts/fehlende Skills/Gaps? Docs/Handoff?).
- [ ] Task 4.5: Push eigenständig — nach Gate, Fork `main`, NIE upstream.
- [ ] Task 4.6: Doku-Update (README/User-Guide/API/ADR wo nötig) + `docs/BACKLOG.md` + `docs/BUGS.md` + Memory-Handoff aktualisieren.

### Verification
- [ ] full-review: keine offenen Critical/High. Honesty-Gate sauber. Auf Fork `main` gepusht. BACKLOG/BUGS/Docs synchron.
