# Implementation Plan: Observability Infrastructure (H-P-09)

**Track ID:** observability_20260601
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-01
**Status:** ⛔ ENTRY-CRITERIA-GATED — [ ] Not Started

## Overview

A 2–3 week dedicated sprint. **Phase 0 is the entry gate** — no instrumentation code is
written until the stack-decision ADR exists. After the gate, instrument one vertical
slice end-to-end, then fan out to metrics, web-vitals, and dashboards/SLOs.

## Phase 0: Entry gate — stack decision (BLOCKING)

### Tasks

- [ ] Task 0.1: Evaluate self-hosted OTel + Tempo + Prometheus + Grafana vs an
      off-the-shelf APM (cost, self-hosting fit, GDPR data-residency, effort).
- [ ] Task 0.2: Record the decision as an ADR (`/architecture-decision-records`). Until
      this lands, the track stays `pending`.

### Verification

- [ ] A stack-decision ADR is committed; the rest of the plan is unblocked.

## Phase 1: Bootstrap + one traced vertical slice

### Tasks

- [ ] Task 1.1: Wire the SDK bootstrap into `src/instrumentation.ts` (alongside the
      existing cron starts); no-PII processor on all attributes.
- [ ] Task 1.2: Instrument one server action → Prisma → Connector fetch path with spans;
      integration test asserts a span is emitted.
- [ ] Task 1.3: Make the scheduler run lifecycle (`RunCoordinator` phases) the parent span.

### Verification

- [ ] One end-to-end trace is visible; the span test passes.

## Phase 2: Metrics (RED + resilience + scheduler)

### Tasks

- [ ] Task 2.1: RED metrics for API routes + server actions (wrapper-level).
- [ ] Task 2.2: Connector circuit-breaker + rate-limiter gauges; scheduler run counts/durations.
- [ ] Task 2.3: Unit tests for the metric wrappers.

### Verification

- [ ] Golden-signal metrics flow to the backend; wrappers tested.

## Phase 3: Core Web Vitals

### Tasks

- [ ] Task 3.1: Client-side LCP/INP/CLS capture → backend ingest endpoint.
- [ ] Task 3.2: Test the ingest endpoint (auth + no-PII).

### Verification

- [ ] Web-vitals appear in the backend.

## Phase 4: Dashboards + SLO/alerts

### Tasks

- [ ] Task 4.1: Golden-signal dashboards (Grafana or APM).
- [ ] Task 4.2: Define ≥1 SLO (e.g. automation-run success rate) + error-budget alert.

### Verification

- [ ] Dashboards live; SLO + alert configured.

## Final Verification

- [ ] Entry-gate ADR committed; tracing + metrics + web-vitals + dashboards in place
- [ ] No PII in any span/metric label (GDPR)
- [ ] `bash scripts/test.sh` green; `bun run build` zero type errors
- [ ] Ready for review

---

_Generated by Conductor. Entry-gated — Phase 0 must clear before any other phase._
