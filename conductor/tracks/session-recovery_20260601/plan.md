# Implementation Plan: Session Recovery & Stale-Session Guard (ROADMAP 3.11)

**Track ID:** session-recovery_20260601
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-01
**Status:** ⛔ ENTRY-CRITERIA-GATED — [ ] Not Started

## Overview

Effort M, scheduled feature. **Phase 0 is the entry gate** (ROADMAP 3.11 prioritised).
Then the stale-session guard (Phase 1), then `usePersistedForm` (Phase 2). TDD; the guard
must not add a per-request query when its cache is warm.

## Phase 0: Entry gate — ROADMAP 3.11 prioritised (BLOCKING)

### Tasks

- [ ] Task 0.1: Confirm ROADMAP 3.11 is scheduled for this sprint. Until then the track
      stays `pending`.

### Verification

- [ ] Scheduling confirmed; the work is unblocked.

## Phase 1: Stale-Session Guard

### Tasks

- [ ] Task 1.1: Unit test: `getCurrentUser()` returns `null` for a JWT id absent from the
      DB, and caches the existence check (~60s TTL, no per-request query when warm) — failing first.
- [ ] Task 1.2: Add the DB-existence check + in-memory cache to `getCurrentUser()`
      (`src/utils/user.utils.ts:5`).
- [ ] Task 1.3: Add the dashboard banner ("session invalid — sign in again") + sign-out
      button on trip; i18n ×4 locales.
- [ ] Task 1.4: Verify no P2003 reaches the user; component test for the banner.

### Verification

- [ ] Stale id → null → "not authenticated"; banner shown; no cryptic FK error; cache works.

## Phase 2: usePersistedForm

### Tasks

- [ ] Task 2.1: Unit test: `usePersistedForm` saves (debounced), restores, clears on
      submit, and respects the 30-min TTL — failing first.
- [ ] Task 2.2: Implement `usePersistedForm` wrapping `react-hook-form`; key
      `jobsync-form-{formId}`; mirror `useKanbanState`/`useStagingLayout` SSR-safe idiom.
- [ ] Task 2.3: Apply to AddJob, AddAutomation, Profile sections, SMTP settings.
- [ ] Task 2.4: E2E happy-path: stale session → banner → re-login → draft restored.

### Verification

- [ ] Drafts persist + restore + clear on submit; the four forms use the hook; E2E passes.

## Final Verification

- [ ] Entry scheduling confirmed; guard + `usePersistedForm` shipped
- [ ] No PII persisted beyond the 30-min TTL; banner i18n ×4 locales
- [ ] `bash scripts/test.sh` green; `bun run build` zero type errors
- [ ] Ready for review

---

_Generated by Conductor. Entry-gated — Phase 0 (ROADMAP 3.11 scheduling) must clear first._

## Phase 3: Wrap-Up (finale Phase — vor Merge/Push)

Per `conductor/workflow.md` § Wrap-Up-Phase. Run only after all prior phases pass.

### Tasks
- [ ] Task 3.1: Blind-Spot-Analyse — projektweit `grep` nach den Pattern-Fixes dieses Tracks; adjacent Lücken schließen.
- [ ] Task 3.2: `/comprehensive-review:full-review` (Architecture+Security+Performance+Testing+Best-Practices) — alle realen Findings autonom fixen; Agent-Claims gegen `git diff`/Code verifizieren (kein Fabrizieren).
- [ ] Task 3.3: `/understand` inkrementell-Refresh + Graph-Commit (1× am Welle-Ende, NICHT per-Commit; `autoUpdate` OFF).
- [ ] Task 3.4: Honesty-Gate voll ausführen (2 Fragen: Shortcuts/fehlende Skills/Gaps? Docs/Handoff?).
- [ ] Task 3.5: Push eigenständig — nach Gate, Fork `main`, NIE upstream.
- [ ] Task 3.6: Doku-Update (README/User-Guide/API/ADR wo nötig) + `docs/BACKLOG.md` + `docs/BUGS.md` + Memory-Handoff aktualisieren.

### Verification
- [ ] full-review: keine offenen Critical/High. Honesty-Gate sauber. Auf Fork `main` gepusht. BACKLOG/BUGS/Docs synchron.
