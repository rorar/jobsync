# Implementation Plan: getStagedVacancies cursor pagination

**Track ID:** cursor-pagination_20260601
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-01
**Status:** ⛔ ENTRY-CRITERIA-GATED — [ ] Not Started

## Overview

~2–3 day focused sprint, **pre-emptive**. **Phase 0 is the entry gate** — do not start
until a user-scale/perf trigger. Then migrate the query to a composite cursor, update the
four consumers, and redefine select-all. TDD; page-boundary correctness is the high-risk
invariant (no duplicate/skipped rows).

## Phase 0: Entry gate — confirm the trigger (BLOCKING)

### Tasks

- [ ] Task 0.1: Confirm a user-scale/perf trigger (large backlog or measured slow deep
      page). If none, the track stays `pending` — do not start.

### Verification

- [ ] A concrete trigger is recorded; the work is justified.

## Phase 1: Cursor query in getStagedVacancies

### Tasks

- [ ] Task 1.1: Unit tests for the composite-cursor query (`discoveredAt`+`id`): first
      page, next page, last page, empty, with filters — failing first.
- [ ] Task 1.2: Replace `skip: offset` with cursor pagination in
      `stagedVacancy.actions.ts:68` (keep `STAGED_VACANCY_LIST_SELECT`, ADR-015 userId);
      preserve tab/status/search filters + `discoveredAt desc` ordering.

### Verification

- [ ] Cursor query passes all page tests; filters intact; no duplicate/skipped rows at boundaries.

## Phase 2: Update the four consumers

### Tasks

- [ ] Task 2.1: Component test: `StagingContainer` paginates via cursor; new-items banner
      computes correctly against cursor ordering.
- [ ] Task 2.2: Migrate `StagingContainer`, `RecordsPerPageSelector`, and
      `StagingNewItemsBanner` to the cursor contract.

### Verification

- [ ] The staging UI paginates via cursor; banner correct; page-size control works.

## Phase 3: Redefine "select all" under cursor pagination

### Tasks

- [ ] Task 3.1: Test: `BulkActionBar` select-all selects all-rows-matching-filter
      (server-resolved), not just the current page.
- [ ] Task 3.2: Implement select-all-matching-filter; wire into the bulk-action path.
- [ ] Task 3.3: Perf sanity check: a deep page does not regress vs the offset baseline.

### Verification

- [ ] Select-all is filter-scoped + tested; deep-page perf improved.

## Final Verification

- [ ] Entry trigger recorded; cursor pagination live; all four consumers migrated
- [ ] Filters/search/ordering intact; select-all filter-scoped; no boundary dupes
- [ ] `bash scripts/test.sh` green; `bun run build` zero type errors
- [ ] Ready for review

---

_Generated by Conductor. Entry-gated + pre-emptive — Phase 0 trigger must exist first._

## Phase 4: Wrap-Up (finale Phase — vor Merge/Push)

Per `conductor/workflow.md` § Wrap-Up-Phase. Run only after all prior phases pass.

### Tasks
- [ ] Task 4.1: Blind-Spot-Analyse — projektweit `grep` nach den Pattern-Fixes dieses Tracks; adjacent Lücken schließen.
- [ ] Task 4.2: `/comprehensive-review:full-review` (Architecture+Security+Performance+Testing+Best-Practices) — alle realen Findings autonom fixen; Agent-Claims gegen `git diff`/Code verifizieren (kein Fabrizieren).
- [ ] Task 4.3: `/understand` inkrementell-Refresh + Graph-Commit (1× am Welle-Ende, NICHT per-Commit; `autoUpdate` OFF).
- [ ] Task 4.4: Honesty-Gate voll ausführen (2 Fragen: Shortcuts/fehlende Skills/Gaps? Docs/Handoff?).
- [ ] Task 4.5: Push eigenständig — nach Gate, Fork `main`, NIE upstream.
- [ ] Task 4.6: Doku-Update (README/User-Guide/API/ADR wo nötig) + `docs/BACKLOG.md` + `docs/BUGS.md` + Memory-Handoff aktualisieren.

### Verification
- [ ] full-review: keine offenen Critical/High. Honesty-Gate sauber. Auf Fork `main` gepusht. BACKLOG/BUGS/Docs synchron.
