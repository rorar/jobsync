# Implementation Plan: PII-at-Rest — Person field-level encryption

**Track ID:** pii-at-rest_20260601
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-01
**Status:** ⛔ ENTRY-CRITERIA-GATED — [ ] Not Started

## Overview

Multi-day, risk-bearing migration. **Phase 0 is the entry gate** — the 5 design
questions must be resolved before any migration code. Then Allium spec, then encrypt
fields behind a reversible backfill, then fix anonymize/merge, then verify search.
TDD throughout; the backfill is the high-risk step (no PII may be lost).

## Phase 0: Entry gate — design phase (BLOCKING)

### Tasks

- [ ] Task 0.1: Resolve design Q1 (which fields encrypt vs stay queryable) and Q2
      (search strategy: blind-index HMAC vs client-side vs normalized-hash column).
- [ ] Task 0.2: Resolve Q3 (key mgmt — reuse `encryption.ts`), Q4 (reversible DRY_RUN
      backfill), Q5 (anonymize/merge interplay). Record an ADR.

### Verification

- [ ] All 5 questions answered + ADR committed; the track is unblocked.

## Phase 1: Allium spec (FIRST — before code)

### Tasks

- [ ] Task 1.1: Extend `crm.allium` / `crm-gdpr.allium` / `security-rules.allium` /
      `api-key-management.allium` with the at-rest encryption rules + search strategy.
- [ ] Task 1.2: `allium:check` clean; freeze as source of truth.

### Verification

- [ ] `allium:check` passes; the spec covers fields, search, anonymize, merge.

## Phase 2: Field encryption + search strategy

### Tasks

- [ ] Task 2.1: Unit tests (encrypt/decrypt round-trip per agreed field) — failing first.
- [ ] Task 2.2: Encrypt the agreed `Person` fields via `encryption.ts` at the repository
      boundary (`person.actions.ts`); decrypt server-only on read.
- [ ] Task 2.3: Implement the chosen search strategy (blind-index / hash column) so CRM
      list/search still works; component/integration test.

### Verification

- [ ] Fields encrypted at rest; search works per strategy; round-trip tested.

## Phase 3: Reversible backfill migration

### Tasks

- [ ] Task 3.1: DRY_RUN-able backfill script (model on `migrate-person-address-country-codes.ts`),
      per-row try/catch; test DRY_RUN + apply on a DB copy.
- [ ] Task 3.2: Verify no row loses data; document the reverse path.

### Verification

- [ ] Existing rows encrypted, no data loss; rollback documented.

## Phase 4: Anonymize / merge on encrypted fields

### Tasks

- [ ] Task 4.1: Regression test: `AnonymizePerson` + `MergePersons` on encrypted data.
- [ ] Task 4.2: Fix `MergePersons` to decrypt→re-encrypt; ensure `AnonymizePerson`
      still clears encrypted fields.

### Verification

- [ ] Anonymize + merge correct on encrypted data; tests pass.

## Final Verification

- [ ] Entry-gate ADR + Allium spec committed; fields encrypted; backfill reversible
- [ ] `allium:weed` zero-drift; anonymize/merge correct; search works
- [ ] `bash scripts/test.sh` green; `bun run build` zero type errors
- [ ] Ready for review

---

_Generated by Conductor. Entry-gated — Phase 0 (design) must clear before any code._
