# Implementation Plan: Welle 4 — Custom JobStatus XL (Kette A)

**Track ID:** welle4-custom-jobstatus_20260601
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-01
**Status:** [ ] Not Started

## Overview

Spec-first (Allium) because this is domain-rule-heavy. Hard ordering: spec → F-AJ-09
(custom status model) → F-AJ-02 (applied-toggle ComboBox) → layout + dynamic Kanban.
TDD; the status migration is the high-risk step (no job may lose its status).

## Phase 1: Allium spec (FIRST — no code yet)

### Tasks

- [ ] Task 1.1: Author `specs/job-status.allium` via `allium:elicit` — JobStatus state
      machine, category model, Kanban-column derivation, API surface, delete-in-use rule.
- [ ] Task 1.2: Validate with `allium:check`; refine with `allium:tend`.
- [ ] Task 1.3: Review the spec against product goals; freeze it as the source of truth.

### Verification

- [ ] `allium:check` passes; the spec covers status CRUD, categories, Kanban, and API.

## Phase 2: F-AJ-09 — User-customisable JobStatus + category (XL)

### Tasks

- [ ] Task 2.1: Unit tests for the state machine + category validation + delete-in-use
      handling (per spec) — failing first.
- [ ] Task 2.2: Prisma migration: custom-status + category models (per user); seed a
      default set; map legacy fixed statuses onto seeded equivalents.
- [ ] Task 2.3: Status-management actions (create/rename/reorder/delete) in the Job-status
      Repository (ADR-015 userId; ADR-019 boundary validation for category union).
- [ ] Task 2.4: Status-management UI (Settings or Job area); i18n (en/de/fr/es).
- [ ] Task 2.5: Build + tests; migrate a DB copy and verify no job lost its status.

### Verification

- [ ] Users manage statuses+categories; existing jobs migrated; spec rules enforced.

## Phase 3: F-AJ-02 — Status ComboBox + applied-toggle (depends on F-AJ-09)

### Tasks

- [ ] Task 3.1: Component test: ComboBox lists the user's custom statuses; applied-toggle
      logic behaves per spec.
- [ ] Task 3.2: Rebuild the status ComboBox against the custom-status source + applied toggle.
- [ ] Task 3.3: i18n; build + tests.

### Verification

- [ ] ComboBox is driven by custom statuses; applied toggle correct.

## Phase 4: F-AJ-03 — Job-form layout reorder

### Tasks

- [ ] Task 4.1: Component test asserting Status renders above Date Applied.
- [ ] Task 4.2: Reorder the Job-form layout (Status above Date Applied).
- [ ] Task 4.3: Build + tests.

### Verification

- [ ] Status field sits above Date Applied; no regression in form behaviour.

## Phase 5: Dynamic Kanban columns + nav badges

### Tasks

- [ ] Task 5.1: Component test: Kanban columns + nav badges derive from custom
      statuses/categories; no hardcoded status list remains.
- [ ] Task 5.2: Replace hardcoded status lists with derivation from the user's statuses;
      keep `useKanbanState` column-order persistence.
- [ ] Task 5.3: E2E happy-path: create a custom status, set it on a job, see the new column.
- [ ] Task 5.4: i18n; build + tests; `allium:weed` for zero spec↔code drift.

### Verification

- [ ] Kanban + badges fully driven by custom statuses; E2E passes; no drift.

## Final Verification

- [ ] All acceptance criteria met (spec, F-AJ-09, F-AJ-02, F-AJ-03, dynamic Kanban/badges)
- [ ] `bash scripts/test.sh` green; `bun run build` zero type errors
- [ ] `allium:weed` reports zero `specs/job-status.allium` ↔ code drift
- [ ] `/architecture-decision-records` run for the custom-status model
- [ ] UI changes reviewed via ui-design agents + responsive check
- [ ] i18n consistent across en/de/fr/es; ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
